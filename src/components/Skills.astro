---
---

<section id="habilidades" class="py-32 px-6 bg-black relative overflow-hidden">
  <div class="max-w-6xl mx-auto relative z-10">
    <!-- Header -->
    <div class="mb-20 text-center">
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-4 smooth-transition">
        Habilidades & Herramientas
      </h2>
      <p class="text-gray-400 text-lg max-w-2xl mx-auto font-light smooth-transition hover:text-gray-300">
        Tecnologías y herramientas que domino para crear soluciones web modernas
      </p>
    </div>

    <!-- Languages Grid -->
    <div class="mb-24">
      <h3 class="text-2xl font-bold text-white mb-8 flex items-center gap-3 smooth-transition">
        <span class="w-1 h-8 bg-accent-primary rounded-full"></span>
        Lenguajes de Programación
      </h3>
      <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
        {
          ['JavaScript', 'TypeScript', 'Python', 'HTML/CSS'].map((lang, i) => (
            <div 
              class="group glass-premium rounded-lg p-6 border border-gray-800 hover:border-accent-primary/50 smooth-transition will-change-transform cursor-pointer h-48"
            >
              <div class="flex items-center justify-between mb-4">
                <div class="text-lg font-semibold text-white smooth-transition group-hover:text-accent-primary">{lang}</div>
                <div class="w-8 h-8 rounded-full bg-accent-primary/10 group-hover:bg-accent-primary/20 flex items-center justify-center smooth-fast">
                  <span class="text-accent-primary text-sm">→</span>
                </div>
              </div>
              <div class="w-full bg-gray-800 rounded-full h-2 overflow-hidden">
                <div class="bg-gradient-to-r from-accent-primary to-accent-secondary h-full smooth-transition" style={`width: ${90 + Math.random() * 10}%`}></div>
              </div>
              <p class="text-xs text-gray-500 mt-3 smooth-transition group-hover:text-gray-400">Dominio avanzado</p>
            </div>
          ))
        }
      </div>
    </div>

    <!-- Frameworks -->
    <div class="mb-24">
      <h3 class="text-2xl font-bold text-white mb-8 flex items-center gap-3 smooth-transition">
        <span class="w-1 h-8 bg-accent-primary rounded-full"></span>
        Frameworks & Librerías
      </h3>
      <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
        {
          ['React', 'Astro', 'Next.js', 'Vue.js'].map((framework, i) => (
            <div 
              class="group glass-premium rounded-lg p-6 border border-gray-800 hover:border-accent-secondary/50 smooth-transition will-change-transform cursor-pointer h-48"
            >
              <div class="flex items-center justify-between mb-4">
                <div class="text-lg font-semibold text-white smooth-transition group-hover:text-accent-secondary">{framework}</div>
                <div class="w-8 h-8 rounded-full bg-accent-secondary/10 group-hover:bg-accent-secondary/20 flex items-center justify-center smooth-fast">
                  <span class="text-accent-secondary text-sm">→</span>
                </div>
              </div>
              <div class="w-full bg-gray-800 rounded-full h-2 overflow-hidden">
                <div class="bg-gradient-to-r from-accent-secondary to-accent-primary h-full smooth-transition" style={`width: ${85 + Math.random() * 15}%`}></div>
              </div>
              <p class="text-xs text-gray-500 mt-3 smooth-transition group-hover:text-gray-400">Experiencia sólida</p>
            </div>
          ))
        }
      </div>
    </div>

    <!-- Tools & Platforms -->
    <div>
      <h3 class="text-2xl font-bold text-white mb-8 flex items-center gap-3 smooth-transition">
        <span class="w-1 h-8 bg-accent-primary rounded-full"></span>
        Herramientas & Plataformas
      </h3>
      <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
        {
          ['Git & GitHub', 'VS Code', 'MongoDB', 'Docker'].map((tool, i) => (
            <div 
              class="group glass-premium rounded-lg p-6 border border-gray-800 hover:border-accent-primary/50 smooth-transition will-change-transform cursor-pointer h-28"
            >
              <div class="flex items-center justify-between">
                <span class="text-lg font-semibold text-white smooth-transition group-hover:text-accent-primary">{tool}</span>
                <span class="text-2xl group-hover:scale-110 smooth-fast inline-block">✓</span>
              </div>
              <p class="text-xs text-gray-500 mt-3 smooth-transition group-hover:text-gray-400">Herramienta clave</p>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
